import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '75%',
        height: 56,
        backgroundColor: '#EA0003',
        borderRadius: 8,
        flexDirection: 'row',
        alignItems: 'center'
    },
    title: {
        flex: 1,
        color: '#f8b700',
        fontSize: 15,
        textAlign: 'center',    
    }
});