import { 
    API, 
    API_AUTH, 
    NAMESPACE, 
    LOCAL, 
    REGION, 
    CLIENT_ID, 
    CLIENT_SECRET,
    LOW_CR,
    COMBATENT,
    DESAFIANT,
    RIVAL,
    DUELIST,
    GLADIATOR

} from "@env"

const environmentVariables = {
    API,
    API_AUTH,
    NAMESPACE,
    LOCAL,
    REGION,
    CLIENT_ID,
    CLIENT_SECRET,
    LOW_CR,
    COMBATENT,
    DESAFIANT,
    RIVAL,
    DUELIST,
    GLADIATOR
}

export default environmentVariables