import { 
    API, 
    API_AUTH, 
    NAMESPACE, 
    LOCAL, 
    REGION, 
    CLIENT_ID, 
    CLIENT_SECRET } from "@env"

const environmentVariables = {
    API,
    API_AUTH,
    NAMESPACE,
    LOCAL,
    REGION,
    CLIENT_ID,
    CLIENT_SECRET
}

export default environmentVariables